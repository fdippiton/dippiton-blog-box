{
  "buildCommand": "cd client-app && yarn install && npx vite build",
  "outputDirectory": "client-app/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
